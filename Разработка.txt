


Не обновляется вывод шкалы scale__item
при изменении полей settings

- 1. Сделать св-во fonts внутри computed
  при открытой в консоли VueDevTools - работает с задержкой на 1 действие
  без консоли - вообще не обновляется

- 2. Сделать при изменении формы вызов ф-и setModualarScale()
  работает с задержкой на 1 действие

- 3. Добавить вывод аттр. key для v-for
  работает с задержкой на 1 действие

4. Выводить блок scale__item  через ф-ю, а не через шаблон html

5. Сделать компонент




Компонент Scale
Error - Нельзя исп-ть v-for внутри компонета, (хотя д-е в него передаются)

Cannot use v-for on stateful component root element because it renders multiple elements.
1  |  
2  |          <div class="scale__item" v-for="font in fonts" _v-bind:key="font.id">


Что делать далее:

0. Создать новый файл (чистый))

1. Settings - сделать компонентом

2. Связать событие в компоненте (изменение полей настроек)
  с экземпляром Root

3. Сделать д-е в компоненте

4. Сделать д-е в Root

5. Вычисление шкалы в Root ?